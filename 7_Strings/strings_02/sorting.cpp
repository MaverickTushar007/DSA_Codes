
#include <iostream>
#include <string>
#include<algorithm>
using namespace std;

int main()
{
    string s, s1, s2;
    // cin>>s;
    // sort(s.begin(), s.end());
    // cout<<s<<endl;

    // cin >> s1;
    // sort(s1.begin(), s1.end());
    // cout << s1;

    getline(cin,s2);
    sort(s2.begin(), s2.end());
    cout << s2;
}

